material-ui-icons default class 
.MuiSvgIcon-root 
it time saving
.sidebar_profileIcons > .MuiSvgIcon-root {
    padding: 10px;
}

firebase
import firebase from "firebase"

const firebaseConfig = {
    apiKey: "AIzaSyAWUvQovP-QRBnsRTmXGqJSm262--NEuAE",
    authDomain: "discord-app-93e49.firebaseapp.com",
    projectId: "discord-app-93e49",
    storageBucket: "discord-app-93e49.appspot.com",
    messagingSenderId: "124409758762",
    appId: "1:124409758762:web:574b010b02270500fbef53",
    measurementId: "G-Q00X1TTZ1D"
  };

  const firebaseApp = firebase.initializeApp(firebaseConfig);
  const db = firebaseApp.firestore();
  const auth = firebase.auth();
  const provider = new firebase.auth.GoogleAuthProvider();


implicit export 
  export {auth,provider};

  explicit export
  export default db

  //to dispatch(save something in redux use dispatch)
  dispatch(setChannelInfo({
            channelId:id,
            channelName:channelname,
            })
        )
        while dispatching we have to import reducer state in appSlice case its setChannelInfo

to use data from redux
useSelector
const user = useSelector(selectUser)
   const channelId = useSelector(selectChannelId)
   const channelName = useSelector(selectChannelName)
   
   we have to import 
   export const selectChannelId = (state) => state.app.channelId;
  export const selectChannelName = (state) => state.app.channelName;  
  both selectors